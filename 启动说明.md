# 金融知识库 RAG 服务 — 启动说明

## 项目简介

- 独立 RAG 知识库服务:文档导入 + 知识问答,从客服后端拆出,通过 HTTP 提供能力
- 监听端口:默认 **18082**(`.env` 的 `APP_PORT` 可改)
- 技术栈:LangGraph 双工作流 / Milvus 2.x / BGE-M3(FlagEmbedding)/ DashScope 重排 /
  MongoDB(对话历史)/ MinIO(备用)
- 查询链路:意图确认 → Milvus 混合检索(dense+sparse)→ RRF 融合 → DashScope reranker
  重排 → LLM 答案生成(qwen-flash,独立于客服主链路 deepseek-v4-flash)

## 启动前置条件

- Python 3.12+、已安装 uv
- 外部服务已部署(Milvus / MongoDB 在 81.70.232.147,**无需本地安装**)
- `.env` 已配置(本机已有;如缺失,从 `.env.example` 复制并填写,必填项缺失启动即报错)

## 启动步骤

### 1. 进入项目目录

```bash
cd /Users/cuixiaojing/Documents/Project_pycharm/finance-customer-service/finance-knowledge-base/finance-knowledge-base
```

### 2. 创建虚拟环境并安装依赖(首次执行)

```bash
uv venv .venv
uv pip install -e . --default-index https://pypi.tuna.tsinghua.edu.cn/simple
```

> ⚠️ 依赖较大:flagembedding 会带 torch,约 2GB,安装较慢属正常。
> 若本机其他项目(如 01_knowledge-base、客服后端)的 `.venv` 已装
> pymilvus 2.4.9 / flagembedding 等,可直接复用该解释器运行,省去重装。

### 3. 确认 .env

```bash
cp .env.example .env   # 仅首次;本机已有 .env 可跳过
```

必填键(缺失启动即报错):`RAG_LLM_API_KEY` / `MILVUS_URL` / `MONGO_URL` / `BGE_M3_PATH` 等。
当前项目 `.env` 已配置完整,直接启动即可。

### 4. 启动服务

```bash
uv run python main.py
```

### 5. 验证

```bash
# 健康检查
curl http://127.0.0.1:18082/health

# 知识问答测试
curl -X POST http://127.0.0.1:18082/api/rag/query \
  -H "Content-Type: application/json" \
  -d '{"question": "个人贷款需要什么条件?", "session_id": "s001"}'
```

## 与客服后端的关系

- 客服后端 `.env` 配 `RAG_SERVICE_URL=http://127.0.0.1:18082` 后,知识轨道走本服务
- `RAG_SERVICE_URL` 为空或本服务不可达时,客服后端自动降级内置 FAQ,不影响主链路
- 启动顺序建议:先启动本服务,再启动客服后端

## 接口一览

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/rag/query` | 知识问答;body `{question, session_id?}` |
| POST | `/api/rag/import` | 重新导入知识文档(幂等);body `{docs_dir?}` |
| GET | `/api/rag/collections` | Milvus 集合与行数(排障) |
| GET | `/health` | 健康检查 |

## 依赖版本坑(勿升级,踩过)

- `pymilvus` 必须 **==2.4.9**:3.x 移除了 `pymilvus.model` 子模块,BGE-M3 嵌入函数无法导入
- `setuptools<81`、`marshmallow<4`:新版移除 `pkg_resources` / `__version_info__`,
  会导致 milvus_model / flagembedding 导入失败

## 排障速查

- **查询返回“未找到”**:不是没路由到 RAG,而是本服务内部某环节失败,看本服务 uvicorn 日志
  (`Extracted topics: []` = LLM 提取失败;401/429 = key 无效/冷却)
- **Milvus 集合检查**:`curl http://127.0.0.1:18082/api/rag/collections`
- **主题名是文件名 fallback**(如 `01_个人贷款业务指南`):`RAG_LLM_API_KEY` 无效所致,
  换有效 key 后重跑 `POST /api/rag/import` 即恢复语义化主题(幂等)
